* not working yetclear set mem 300minsheet using "/Users/akeil/School/Dissertation/Radiation/RERF/lssinc07/mylss.csv", clear *poisson model		program define poisson			version 10.0			args lnf b1 b0			tempvar eta sigma mu			gen double `eta' = exp(`b0')			gen double `sigma' = `eta'*(1+`b1')			gen double `mu' = `sigma'			quietly replace `lnf' = $ML_y1*ln(`mu')- `mu' - lnfact($ML_y1)		end			ml model lf mllinrr (b1: kidney = blaa02w10) (b0: male female naga nic, nocons)	ml check	    	ml max	program drop mllinrr